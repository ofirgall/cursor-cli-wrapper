[package]
name = "cursor-cli-wrapper"
version = "0.1.0"
edition = "2024"

[dependencies]
crossterm = "0.29.0"
libc = "0.2.181"
pty-process = { version = "0.5.3", features = ["async"] }
tokio = { version = "1", features = ["full"] }
